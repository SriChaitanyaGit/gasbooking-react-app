apiVersion: apps/v1
kind: Deployment # Kubernetes resource kind we are creating
metadata:
  name: gbs-fe-app-dep
spec:
  selector:
    matchLabels:
      app: gbs-fe-app
  replicas: 2 # Number of replicas that will be created for this deployment
  template:
    metadata:
      labels:
        app: gbs-fe-app
    spec:
      containers:
        - name: gbs-fe-app
          image: rolla777get/gbs-fe-app:latest # Image that will be used to containers in the cluster
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5173 # The port that the container is running on in the cluster